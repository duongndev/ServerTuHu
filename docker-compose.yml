version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: duongnd202/tuhu-bread:latest
    container_name: tuhu_bread_app
    env_file:
      - .env
    ports:
      - "5000:5000"
    restart: always